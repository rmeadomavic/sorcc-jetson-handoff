# Internal-only working folders — must never be committed to the public repo.
# Brand specs, design exports, internal notes live here locally.
brand/
_dev/
_internal/

# Local artifacts
*.log
*.swp
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Build artifacts (none expected, but just in case)
*.tar.gz
*.zip

# --- Secrets ---
.env
.env.*
!.env.example
*.pem
*.key
*_secret
*_token
